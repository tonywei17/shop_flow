# ===========================================
# SECURITY WARNING: Never commit real secrets!
# ===========================================

# Supabase (Server-side only - NEVER expose SERVICE_ROLE_KEY to client)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Stripe (Server-side only)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Langflow
LANGFLOW_BASE_URL=
LANGFLOW_API_KEY=

# App URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# Admin Authentication (CRITICAL - use strong values in production)
# ===========================================
# Super admin account ID
ADMIN_LOGIN_ID=admin
# Password: Use a strong password (min 12 chars, mixed case, numbers, symbols)
ADMIN_LOGIN_PASSWORD=change-this-in-production
# Session secret: Must be at least 32 characters, random string
# Generate with: openssl rand -base64 32
ADMIN_SESSION_SECRET=generate-a-random-32-char-string-here

# ⚠️ CRITICAL SECURITY: Admin Clear Data Password (REQUIRED)
# This password protects dangerous data deletion operations
# MUST be set in production - no default value
# Generate with: openssl rand -base64 32
ADMIN_CLEAR_DATA_PASSWORD=your-secure-clear-data-password-here

# ===========================================
# Storefront Authentication
# ===========================================
# Session secret: Must be at least 32 characters, random string
STOREFRONT_SESSION_SECRET=generate-another-random-32-char-string

# ===========================================
# Email (for sending credentials)
# ===========================================
GMAIL_USER=
GMAIL_APP_PASSWORD=
STOREFRONT_URL=https://your-store-domain.com
